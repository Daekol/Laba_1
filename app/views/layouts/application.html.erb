<!DOCTYPE html>
<html>
  <head>
    <title>Lab6</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="d-flex flex-column flex-md-row align-items-center p-3 px-md-4 mb-3 bg-white border-bottom shadow-sm">
      <div id="user-info">
        <% if current_user %>
          <%= current_user.email %>
          <%= button_to "Log out", destroy_user_session_path, method: :delete, data: { turbo: false }, class: "btn btn-danger" %>
        <% end %>
      </div>
      <h5 class="my-0 mr-md-auto font-weight-bold">Lab 9</h5>
      <nav class="my-2 my-md-0 mr-md-3">
        <span class="p-2 text-dark"><%= link_to "Головна сторінка", page_path %></span>
        <span class="p-2 text-dark"><%= link_to "Команди", teams_path %></span>
        <span class="p-2 text-dark"><%= link_to "Гравці", players_path %></span>
      </nav>
      <div>
        <span class="p-2 text-dark"><%= link_to t('language.switch_to_uk'), set_locale_path(locale: 'uk') %></span>
        <span class="p-2 text-dark"><%= link_to t('language.switch_to_en'), set_locale_path(locale: 'en') %></span>
      </div>
    </div>
    <div class="container">
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
      <%= yield %>
    </div>
  </body>
</html>
